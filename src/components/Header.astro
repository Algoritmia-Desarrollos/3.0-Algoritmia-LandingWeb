---
---

<header class="fixed top-0 left-0 right-0 z-50 flex justify-center pt-6 px-4 pointer-events-none">
  <div class="pointer-events-auto glass-nav border border-white/20 shadow-lg shadow-black/5 rounded-full px-14 py-3 flex items-center gap-6 md:gap-12 transition-all duration-300 ring-1 ring-black/5">
    <a href="#" class="flex items-center gap-2 text-primary hover:opacity-80 transition-opacity">
      <span class="material-symbols-outlined text-[20px]">token</span>
      <span class="text-sm font-semibold tracking-tight">Algoritmia</span>
    </a>
    <nav class="hidden md:flex items-center gap-8">
      <a class="text-[13px] font-medium text-primary/80 hover:text-primary transition-colors" href="#work">Proyectos</a>
      <a class="text-[13px] font-medium text-primary/80 hover:text-primary transition-colors" href="#services">Servicios</a>
      <a class="text-[13px] font-medium text-primary/80 hover:text-primary transition-colors" href="#philosophy">Filosofía</a>
    </nav>
    <div class="flex items-center gap-4 pl-2">
      <a class="hidden md:flex bg-primary text-white px-5 py-2 rounded-full text-[13px] font-medium hover:bg-black/90 transition-all shadow-md shadow-primary/10" href="#contact">
        Hablemos
      </a>
      <button id="mobile-menu-btn" class="md:hidden text-primary p-1">
        <span class="material-symbols-outlined text-[20px]">menu</span>
      </button>
    </div>
  </div>
</header>

<style>
  .glass-nav {
    background: rgba(255, 255, 255, 0.75);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
</style>

<script>
  // Mobile menu logic can be added here if needed, for now just a placeholder
  const btn = document.getElementById('mobile-menu-btn');
  btn?.addEventListener('click', () => {
    // Implement mobile menu toggle
    console.log('Toggle menu');
  });
</script>
