---
---

<header class="fixed top-0 left-0 right-0 z-50 flex justify-center pt-4 px-4 pointer-events-none">
  <div class="pointer-events-auto glass-nav border border-white/20 shadow-lg shadow-black/5 rounded-full px-6 md:px-8 py-3 w-full max-w-[95%] flex items-center justify-between transition-all duration-300 ring-1 ring-black/5">
    
    <!-- Logo (Left) -->
    <a href="#" class="flex items-center gap-2 text-primary hover:opacity-80 transition-opacity shrink-0">
      <span class="material-symbols-outlined text-[24px]">token</span>
      <span class="text-lg font-semibold tracking-tight">Algoritmia</span>
    </a>

    <!-- Nav (Center) -->
    <nav class="hidden md:flex items-center gap-8 absolute left-1/2 -translate-x-1/2">
      <a class="text-sm font-medium text-primary/80 hover:text-primary transition-colors" href="#work">Proyectos</a>
      <a class="text-sm font-medium text-primary/80 hover:text-primary transition-colors" href="#services">Servicios</a>
      <a class="text-sm font-medium text-primary/80 hover:text-primary transition-colors" href="#philosophy">Filosofía</a>
    </nav>

    <!-- Actions (Right) -->
    <div class="flex items-center gap-4 shrink-0">
      <a class="hidden md:flex bg-primary text-white px-6 py-2.5 rounded-full text-sm font-medium hover:bg-black/90 transition-all shadow-md shadow-primary/10" href="#contact">
        Hablemos
      </a>
      <button id="mobile-menu-btn" class="md:hidden text-primary p-1">
        <span class="material-symbols-outlined text-[24px]">menu</span>
      </button>
    </div>

  </div>
</header>

<style>
  .glass-nav {
    background: rgba(255, 255, 255, 0.75);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
</style>

<script>
  // Mobile menu logic can be added here if needed, for now just a placeholder
  const btn = document.getElementById('mobile-menu-btn');
  btn?.addEventListener('click', () => {
    // Implement mobile menu toggle
    console.log('Toggle menu');
  });
</script>
