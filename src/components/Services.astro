---
---
<section id="servicios" class="reveal py-section bg-dark text-white">
    <div class="container">
        <div class="header-grid mb-4">
            <div>
                <p class="text-uppercase text-inverse ls-1">Nuestros Servicios</p>
                <h2 class="text-display text-white">Todo lo que necesitas.<br>En un solo lugar.</h2>
            </div>
            <div class="flex-end">
                <p class="text-lg text-inverse max-w-600 text-right-desktop">
                    Dejamos de lado las agencias fragmentadas. 
                    Unificamos estrategia, diseño y tecnología para escalar tu negocio.
                </p>
            </div>
        </div>

        <div class="bento-grid">
            <!-- Card 1: Web (Large) -->
            <div class="bento-card card-web">
                <div class="card-content">
                    <div class="icon-wrapper"><i class="ph ph-desktop"></i></div>
                    <h3 class="text-h3 mb-2">Desarrollo Web & Apps</h3>
                    <p class="text-inverse text-sm">
                        No hacemos "páginas bonitas". Construimos activos digitales de alto rendimiento. 
                        Velocidad extrema, SEO técnico impecable y UX diseñada para convertir visitas en clientes.
                    </p>
                    <ul class="feature-list mt-4">
                        <li><i class="ph ph-check"></i> E-Commerce Scalable</li>
                        <li><i class="ph ph-check"></i> Landing Pages de Conversión</li>
                        <li><i class="ph ph-check"></i> Web Apps a Medida</li>
                    </ul>
                </div>
                <div class="card-visual visual-code">
                    <div class="code-block">
                        <span>const</span> <span>growth</span> = <span>new</span> <span>Result</span>();
                    </div>
                </div>
            </div>

            <!-- Card 2: Marketing (Large) -->
            <div class="bento-card card-marketing">
                <div class="card-content">
                    <div class="icon-wrapper"><i class="ph ph-trend-up"></i></div>
                    <h3 class="text-h3 mb-2">Growth Marketing</h3>
                    <p class="text-inverse text-sm">
                        Dejamos de quemar presupuesto. Implementamos embudos de venta validados 
                        y campañas de publicidad paga (Ads) con retorno de inversión positivo.
                    </p>
                    <div class="stats-mini mt-4">
                        <div>
                            <span class="stat-num">+300%</span>
                            <span class="stat-label">ROAS Promedio</span>
                        </div>
                        <div>
                            <span class="stat-num">-40%</span>
                            <span class="stat-label">CPA</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Card 3: Branding (Tall/Square) -->
            <div class="bento-card card-branding">
                <div class="card-content">
                    <div class="icon-wrapper"><i class="ph ph-paint-brush"></i></div>
                    <h3 class="text-h3 mb-2">Branding</h3>
                    <p class="text-inverse text-sm">
                        Identidad visual que te diferencia del ruido. 
                        Logotipos, sistemas de diseño y manuales de marca.
                    </p>
                </div>
                <div class="card-visual visual-circle"></div>
            </div>

            <!-- Card 4: Content (Tall/Square) -->
            <div class="bento-card card-content-creation">
                <div class="card-content">
                    <div class="icon-wrapper"><i class="ph ph-video-camera"></i></div>
                    <h3 class="text-h3 mb-2">Contenido</h3>
                    <p class="text-inverse text-sm">
                        Producción audiovisual que conecta. 
                        Reels, fotografía y copy persuasivo.
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .bg-dark { background-color: #050505; color: white; }
    .text-inverse { color: #a1a1a6; }
    .text-right-desktop { text-align: left; }
    
    .header-grid {
        display: grid;
        gap: 2rem;
        margin-bottom: 4rem;
    }

    .bento-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }

    .bento-card {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 24px;
        padding: 2rem;
        position: relative;
        overflow: hidden;
        transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    .bento-card:hover {
        background: rgba(255, 255, 255, 0.08);
        border-color: rgba(255, 255, 255, 0.4);
        transform: translateY(-4px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        z-index: 2;
    }

    .icon-wrapper {
        width: 50px;
        height: 50px;
        background: white;
        color: black;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        margin-bottom: 1.5rem;
    }

    .feature-list { list-style: none; padding: 0; }
    .feature-list li { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 0.5rem; font-size: 0.9rem; color: #ccc; }
    .feature-list i { color: white; }

    .stats-mini { display: flex; gap: 2rem; }
    .stat-num { display: block; font-size: 1.5rem; font-weight: 700; color: white; }
    .stat-label { font-size: 0.75rem; color: #888; text-transform: uppercase; }

    /* Visuals */
    .visual-code {
        margin-top: 2rem;
        background: #111;
        padding: 1rem;
        border-radius: 8px;
        font-family: monospace;
        font-size: 0.9rem;
        opacity: 0.7;
        border: 1px solid #333;
    }
    .visual-code span:nth-child(1) { color: #ff79c6; }
    .visual-code span:nth-child(2) { color: #8be9fd; }
    .visual-code span:nth-child(3) { color: #ff79c6; }
    .visual-code span:nth-child(4) { color: #f1fa8c; }

    .visual-circle {
        width: 100px;
        height: 100px;
        background: radial-gradient(circle, rgba(255,255,255,0.2) 0%, rgba(0,0,0,0) 70%);
        border-radius: 50%;
        position: absolute;
        bottom: -20px;
        right: -20px;
        filter: blur(20px);
    }

    @media (min-width: 768px) {
        .header-grid { grid-template-columns: 1fr 1fr; align-items: end; }
        .text-right-desktop { text-align: right; }
        
        .bento-grid {
            grid-template-columns: repeat(3, 1fr);
            grid-template-rows: repeat(2, minmax(300px, auto));
        }

        .card-web { grid-column: 1 / 3; grid-row: 1 / 2; }
        .card-branding { grid-column: 3 / 4; grid-row: 1 / 2; }
        .card-marketing { grid-column: 1 / 3; grid-row: 2 / 3; }
        .card-content-creation { grid-column: 3 / 4; grid-row: 2 / 3; }
    }
</style>
