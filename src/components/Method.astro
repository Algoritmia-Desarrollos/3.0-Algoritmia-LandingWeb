---
---
<section id="metodo" class="reveal py-section relative">
    <div class="container">
        <div class="text-center max-w-800 mx-auto mb-4">
            <p class="text-uppercase">MÉTODO ALGORITMIA</p>
            <h2 class="text-h2 mb-2">Cómo escalamos tu negocio</h2>
            <p class="text-lg">
                No improvisamos. <strong>No prometemos lo que no se puede cumplir.</strong>
            </p>
        </div>

        <div class="timeline-wrapper">
            <div class="timeline-line"></div>

            <div class="timeline-item reveal">
                <div class="timeline-marker">1</div>
                <div class="timeline-content">
                    <div class="timeline-icon"><i class="ph ph-magnifying-glass"></i></div>
                    <h3 class="text-h3 mb-2">Diagnóstico</h3>
                    <p class="text-sm text-secondary">
                        Auditoría profunda. Entendemos tus números antes de ejecutar.
                    </p>
                </div>
            </div>

            <div class="timeline-item reveal">
                <div class="timeline-marker">2</div>
                <div class="timeline-content">
                    <div class="timeline-icon"><i class="ph ph-clipboard-text"></i></div>
                    <h3 class="text-h3 mb-2">Viabilidad</h3>
                    <p class="text-sm text-secondary">
                        Validamos tu potencial. Si no es rentable, te lo decimos.
                    </p>
                </div>
            </div>

            <div class="timeline-item reveal">
                <div class="timeline-marker">3</div>
                <div class="timeline-content">
                    <div class="timeline-icon"><i class="ph ph-strategy"></i></div>
                    <h3 class="text-h3 mb-2">Plan de Acción</h3>
                    <p class="text-sm text-secondary">
                        Hoja de ruta exacta. Inversión, tiempos y retorno.
                    </p>
                </div>
            </div>

            <div class="timeline-item reveal">
                <div class="timeline-marker">4</div>
                <div class="timeline-content">
                    <div class="timeline-icon"><i class="ph ph-rocket-launch"></i></div>
                    <h3 class="text-h3 mb-2">Escalado</h3>
                    <p class="text-sm text-secondary">
                        Lanzamos campañas que venden y optimizamos con datos.
                    </p>
                </div>
            </div>

        </div>

        <div class="guarantee-block reveal text-center max-w-800 mx-auto mt-8">
            <i class="ph ph-seal-check" style="font-size: 3rem; color: var(--color-black); margin-bottom: 1rem;"></i>
            <h3 class="text-h3 mb-2">Garantía de Resultados</h3>
            <p class="text-h4">
                Si no alcanzamos el objetivo, <strong class="text-highlight-temp">te devolvemos el dinero.</strong>
            </p>
        </div>
    </div>
</section>

<style>
    .timeline-wrapper {
        position: relative;
        max-width: 1000px;
        margin: 0 auto;
        padding: 0rem 0;
    }

    .timeline-line {
        position: absolute;
        left: 20px;
        top: 0;
        bottom: 0;
        width: 2px;
        background: linear-gradient(to bottom, transparent, var(--border-light), transparent);
        z-index: 1;
    }

    .timeline-item {
        display: flex;
        gap: 1.5rem;
        margin-bottom: 2rem;
        position: relative;
        align-items: center;
        transition: all 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        
        /* Reveal Logic */
        opacity: 0;
        transform: translateY(30px);
    }

    .timeline-item.visible {
        opacity: 1;
        transform: translateY(0);
    }

    /* Hover Effect */
    .timeline-wrapper .timeline-item:hover {
        transform: scale(1.02);
        z-index: 10;
    }

    .timeline-marker {
        flex-shrink: 0;
        width: 40px;
        height: 40px;
        background: var(--bg-body);
        border: 1px solid var(--border-light);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 800;
        font-size: 0.9rem;
        color: var(--text-secondary);
        z-index: 10; /* Ensure it's above the line */
        position: relative;
        box-shadow: 0 0 0 8px var(--bg-body);
        transition: all 0.3s ease;
    }

    .timeline-item:hover .timeline-marker {
        background: var(--color-black);
        color: var(--color-white);
        border-color: var(--color-black);
    }

    .timeline-content {
        background: transparent;
        padding: 1rem 0;
        width: 100%;
        transition: all 0.3s ease;
        position: relative;
    }
    
    .timeline-item:hover .timeline-content {
        transform: translateY(-5px);
    }

    .timeline-icon {
        font-size: 1.8rem;
        margin-bottom: 0.5rem;
        color: var(--text-secondary);
        transition: color 0.3s ease;
    }

    @media (min-width: 1024px) {
        .timeline-wrapper {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 2rem;
            padding-top: 3rem; /* Space for the line */
            max-width: 1200px;
        }

        .timeline-line { 
            left: 0; 
            right: 0;
            top: 20px; /* Align with markers */
            width: 100%;
            height: 2px;
            background: linear-gradient(to right, transparent, var(--border-light) 10%, var(--border-light) 90%, transparent);
            transform: none;
        }

        .timeline-item { 
            width: 100%; 
            margin-bottom: 0; 
            padding: 0;
            flex-direction: column;
            align-items: center;
            text-align: center;
        }

        .timeline-marker { 
            width: 50px; 
            height: 50px; 
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
            z-index: 10;
        }

        /* Reset odd/even logic from mobile */
        .timeline-item:nth-child(odd),
        .timeline-item:nth-child(even) {
            margin: 0;
            padding: 0;
            flex-direction: column;
            text-align: center;
        }

        .timeline-item:nth-child(odd) .timeline-marker,
        .timeline-item:nth-child(even) .timeline-marker {
            position: static; /* Reset absolute positioning */
        }

        .timeline-content {
            padding: 2rem 1.5rem;
        }
    }
</style>
